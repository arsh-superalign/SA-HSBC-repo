# Component — Source Pills

**Purpose**
Citations, compactly.

**Used on**
Chat, on answered turns.

**Variants**
None.

**States**
Default · hover.

**Interactions**
Each pill navigates to that page detail view; the full title is a native tooltip. The trace trigger sits at the right end of the same row.

**Important visual rules**
- 1px `#E5E5E5`, max-width 230px, truncated, with a 10px monospace index digit.
- The withheld count is **plain amber text, not a badge**, with the tooltip "Withheld pages are counted, never named".
- This replaced a full bordered sources card — do not reintroduce the card.

---

Tokens: `../../DESIGN_TOKENS.md` · Full component set: `../../COMPONENTS.md` ·
Interaction model: `../../INTERACTIONS.md`
